/**
 * PreviewApp — fullscreen preview of the MPI project.
 *
 * Layer: preview
 * Allowed imports: ../core, ../components
 *
 * STUB for M0–M1. Implementation lands in M5 (Button + Preview).
 * Do NOT implement before M5 — keep milestone scope clean.
 */

export function PreviewApp(): null {
  // Intentionally empty. M5 will replace this with a real preview component.
  return null;
}
