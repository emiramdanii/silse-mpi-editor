/**
 * Export HTML module.
 *
 * Layer: export
 * Allowed imports: ../core
 *
 * STUB for M0–M1. Implementation lands in M6 (Export HTML).
 * See docs/EXPORT_HTML_CONTRACT.md for the output contract.
 *
 * Do NOT implement before M6.
 */

import type { SimpleProject } from '../core/types';

/**
 * Render a SimpleProject as a standalone HTML string.
 *
 * TODO(M6): implement according to docs/EXPORT_HTML_CONTRACT.md.
 */
export function exportProjectToHtml(_project: SimpleProject): string {
  throw new Error('exportProjectToHtml not implemented — lands in M6.');
}
