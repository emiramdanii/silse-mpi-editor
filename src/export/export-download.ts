/**
 * Export download helper.
 *
 * Layer: export
 * Allowed imports: ../core
 *
 * STUB for M0–M1. Implementation lands in M6.
 * Will trigger a browser download of the exported HTML file.
 */

export function downloadHtmlFile(_filename: string, _html: string): void {
  throw new Error('downloadHtmlFile not implemented — lands in M6.');
}
